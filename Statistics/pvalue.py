from Statistics.zscore import z_score


def p_value(data):
    data = []
    num = len(data)
    score = z_score()
    for index in num:
        if num == score:
            data.append(index)

    return index
